import type { PageLoad } from "./$types";

export const load: PageLoad = () => ({
  seo: {
    title: "Research — Lightkeepers dev",
    description:
      "Structured TC research ask (Build topic) against lightkeepers-agent.",
  },
});
